[pytest]
markers =
    extra: tests for optional dependencies/modules
    no_extra: tests to run when optional dependencies are not installed
    no_zstd: tests to run when zstd modules not installed (included in no_extra)
